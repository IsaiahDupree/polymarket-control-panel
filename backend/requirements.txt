# Core API
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
python-dotenv>=1.0

# The trading stack (py_clob_client_v2, edgeos, webshare) is NOT pip-installable;
# the panel reuses an existing venv that has it. Point run.sh at that python via
# PANEL_PYBIN, or run read-only without it.

# Tests
pytest>=8.0
